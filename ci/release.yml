---
# release definition for Concourse.ci
platform: linux
image_resource:
  type: docker-image
  source:
    repository: golang

inputs:
  - name: terraform-provider-gateway

outputs:
  - name: release-binaries

run:
  path: ./terraform-provider-gateway/ci/release-build.sh
